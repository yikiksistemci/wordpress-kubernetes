apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: wordpress
  name: wp-pv-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 9Gi
  selector:
    matchLabels:
      app: wordpress
      tier: wordpress


